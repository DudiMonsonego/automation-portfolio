name: Run Tests
on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          pip install pytest pytest-playwright pytest-html requests pytest-base-url
          playwright install --with-deps

      - name: Create Reports Directory
        run: mkdir -p reports/screenshots

      - name: Run tests
        run: python -m pytest --headless --html=reports/report.html --self-contained-html
        continue-on-error: true # מאפשר להמשיך לשלב השמירה גם אם הטסט נכשל

      - name: Upload Test Report
        if: always() # מבטיח שהדוח יישמר גם אם היו שגיאות
        uses: actions/upload-artifact@v4
        with:
          name: test-report
          path: reports/